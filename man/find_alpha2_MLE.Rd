% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nuisance.R
\name{find_alpha2_MLE}
\alias{find_alpha2_MLE}
\title{Maximum-likelihood fit of the eta2 model C | Z}
\usage{
find_alpha2_MLE(
  w_data,
  delta_data,
  z_c_data = NULL,
  z_d_data = NULL,
  w_min = 0,
  w_max = 12,
  verbose = FALSE
)
}
\arguments{
\item{w_data, delta_data}{Observed times and event indicators (length n).}

\item{z_c_data}{Optional continuous covariates (length-n vector or n-by-p matrix).}

\item{z_d_data}{Optional discrete covariates (length-n vector or n-by-q matrix).}

\item{w_min, w_max}{Truncation bounds for C.}

\item{verbose}{If \code{TRUE}, report the \code{optim} convergence
message. Defaults to \code{FALSE}.}
}
\value{
A named numeric vector: the coefficient estimates alpha2_hat
  (intercept, then z_c then z_d) followed by tau2_hat.
}
\description{
Maximum-likelihood fit of the eta2 model C | Z
}
\examples{
set.seed(1)
n  <- 200
x  <- truncnorm::rtruncnorm(n, a = -1, b = 1, mean = 0, sd = 1)
cc <- truncnorm::rtruncnorm(n, a = -1, b = 1, mean = 1, sd = 1)
w  <- pmin(x, cc)
delta <- as.integer(x <= cc)
y  <- 3 * x + rnorm(n)          # beta = c(0, 3)

# Recovers the mean and SD of the censoring time C; here mean 1, sd 1.
find_alpha2_MLE(w, delta, w_min = -1, w_max = 1)
}
