% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_helpers.R
\name{r2}
\alias{r2}
\title{Residual r2 = |Y - m0(W)|}
\usage{
r2(y, w, delta, beta, alpha1_star, tau1, z_c = NULL, z_d = NULL, w_min, w_max)
}
\arguments{
\item{y, w, delta}{Observed outcome, time, and event indicator.}

\item{beta}{Outcome coefficient vector.}

\item{alpha1_star, tau1}{Present for a common signature with r1; unused.}

\item{z_c, z_d}{Optional covariates.}

\item{w_min, w_max}{Present for a common signature with r1; unused.}
}
\value{
The absolute residual |y - m0(w)|.
}
\description{
Residual r2 = |Y - m0(W)|
}
\examples{
set.seed(1)
n  <- 200
x  <- truncnorm::rtruncnorm(n, a = -1, b = 1, mean = 0, sd = 1)
cc <- truncnorm::rtruncnorm(n, a = -1, b = 1, mean = 1, sd = 1)
w  <- pmin(x, cc)
delta <- as.integer(x <= cc)
y  <- 3 * x + rnorm(n)          # beta = c(0, 3)

beta <- c(0, 3)
i <- which(delta == 0)[1]

# r2 plugs w in for x even when censored, so it never touches the
# X | Z model -- unlike r1, which integrates over X when delta == 0.
r2(y[i], w[i], 0L, beta, alpha1_star = 0, tau1 = 1, w_min = -1, w_max = 1)
r1(y[i], w[i], 0L, beta, alpha1_star = 0, tau1 = 1, w_min = -1, w_max = 1)

# When the event is observed the two agree exactly
j <- which(delta == 1)[1]
r2(y[j], w[j], 1L, beta, alpha1_star = 0, tau1 = 1, w_min = -1, w_max = 1)
r1(y[j], w[j], 1L, beta, alpha1_star = 0, tau1 = 1, w_min = -1, w_max = 1)
}
